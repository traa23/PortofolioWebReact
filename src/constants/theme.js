/** Theme system constants. */

export const THEMES = Object.freeze({
    LIGHT: 'light',
    DARK: 'dark',
    SYSTEM: 'system',
})

/** Brand identity is dark-first. */
export const DEFAULT_THEME = THEMES.DARK

export const THEME_STORAGE_KEY = 'portfolio-theme'
