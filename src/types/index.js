/**
 * Shared JSDoc type definitions (single source of truth for data shapes).
 * Prepared for future TypeScript migration. Concrete data typedefs are
 * added alongside the `data/` files in a later stage.
 */

/**
 * @typedef {'light'|'dark'|'system'} ThemePreference
 */

export { }
