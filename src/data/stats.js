/**
 * Statistics — fill these in. Structure only, no defaults.
 * Keep numbers honest and defensible.
 *
 * @typedef {Object} Stat
 * @property {string} id
 * @property {string} label
 * @property {number} value
 * @property {string} [suffix]  e.g. '+', 'k', '%'
 * @property {'projects'|'certificates'|'technologies'|'learning'} icon
 *
 * @type {Stat[]}
 */
export const stats = []
