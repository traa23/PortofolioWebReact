/**
 * Social links.
 * Shape: { id: string, label: string, href: string, icon: string }
 * `icon` maps to a key in the footer/contact icon registry.
 * Fill in your real profiles here.
 * @type {Array<{ id: string, label: string, href: string, icon: string }>}
 */
export const socials = []
