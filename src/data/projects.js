/**
 * Projects — fill these in. Structure only, no defaults.
 * Put real screenshots in public/projects/ and reference them in `image`
 * (e.g. '/projects/my-app.webp'). Leave `image` empty to show a gradient
 * placeholder. Omit `github`/`demo` to hide that button.
 *
 * @typedef {Object} Project
 * @property {string}  id
 * @property {string}  title
 * @property {string}  description
 * @property {string}  [image]
 * @property {string}  [badge]
 * @property {Array<{ name: string, icon: string }>} tech
 * @property {string}  [github]
 * @property {string}  [demo]
 * @property {boolean} featured
 *
 * @type {Project[]}
 */
export const projects = []
