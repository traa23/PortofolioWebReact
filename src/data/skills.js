/**
 * Skills — fill these in. Structure only, no defaults.
 *
 * @typedef {Object} Skill
 * @property {string} name        Display name (the real information).
 * @property {string} icon        Key into the tech icon registry.
 *
 * @typedef {Object} SkillCategory
 * @property {string} id
 * @property {string} title
 * @property {'frontend'|'backend'|'database'|'ai'|'tools'|'languages'} icon
 * @property {'hero'|'wide'|'normal'} span   Bento tile size.
 * @property {Skill[]} skills
 *
 * @type {SkillCategory[]}
 */
export const skills = []
